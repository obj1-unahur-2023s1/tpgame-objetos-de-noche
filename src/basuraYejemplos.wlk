//DE BLOQUES
/*
	method newPosition(){
		const posicion = self.posicionAleatoria()
		escenario.posiciones().add( posicion )
		return (posicion)
	}
	method posicionAleatoria(){
		return (
			game.at((1..escenario.width()).anyOne(),(1..escenario.height()).anyOne())
		)
	}
	* 
	method explotar(){
		game.onTick(500,"explotar", {
			3.times({i=>image=("explosion_muro_suave_"+i+".png")})
			game.removeTickEvent("explotar") } )
	}  
*/	